<UserControl x:Class="JediTournamentWPF.UserControls.NewTournamentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodel="JediTournamentWPF.Models.NewTournamentModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <CheckBox Name="random" Content="Aléatoire/Déterminé" HorizontalAlignment="Left" Margin="59,50,0,0" VerticalAlignment="Top" Height="27" Width="127"/>
        <TextBox HorizontalAlignment="Left" Height="23" Margin="59,192,0,0" TextWrapping="Wrap" Text="{Binding Random}" VerticalAlignment="Top" Width="120"/>
        <TextBox HorizontalAlignment="Left" Height="23" Margin="59,233,0,0" TextWrapping="Wrap" Text="{Binding Random}" VerticalAlignment="Top" Width="120"/>
        <Button Content="Button" HorizontalAlignment="Left" Margin="215,157,0,0" VerticalAlignment="Top" Width="75"
                Command="{Binding Path=AddCommand}" 
                />
    </Grid>
</UserControl>
